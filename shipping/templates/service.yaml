apiVersion: v1
kind: Service
metadata:
    name: shipping
    namespace: roboshop
    labels:
        project: roboshop
        tier: backend
        service: shipping
spec:
    selector:
        project: roboshop
        tier: backend
        service: shipping
    ports:
        - protocol: TCP
          port: 8080
          targetPort: 8080